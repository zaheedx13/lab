carrepo

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <!-- Standard POM model version -->
  <modelVersion>4.0.0</modelVersion>

  <groupId>SEGroup.bookcar</groupId>
  <artifactId>bookcar</artifactId>

  <!-- Build a WAR for Tomcat -->
  <packaging>war</packaging>

  <version>0.0.1-SNAPSHOT</version>
  <name>bookcar Maven Webapp</name>
  <url>https://github.com/anujyog1/carrepo</url>

  <!-- Project-wide properties -->
  <properties>
    <!-- Target Java LTS version -->
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <maven.compiler.release>17</maven.compiler.release>

    <!-- Plugin versions -->
    <maven.compiler.plugin.version>3.10.1</maven.compiler.plugin.version>
    <maven.surefire.plugin.version>3.0.0-M7</maven.surefire.plugin.version>
    <maven.war.plugin.version>3.3.2</maven.war.plugin.version>
  </properties>

  <dependencies>
    <!-- Servlet API for compile-time only (Tomcat provides runtime implementation) -->
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>javax.servlet-api</artifactId>
      <version>4.0.1</version>
      <scope>provided</scope>
    </dependency>

    <!-- Updated JUnit (JUnit 4) for tests -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- Control WAR filename -->
    <finalName>bookcar</finalName>

    <plugins>
      <!-- Compiler plugin: use 'release' for correct platform APIs -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>

      <!-- Surefire for running tests -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${maven.surefire.plugin.version}</version>
      </plugin>

      <!-- WAR plugin (standard) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>${maven.war.plugin.version}</version>
        <configuration>
          <!-- exclude provided dependencies from WAR -->
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <!-- Optional SCM metadata (teacher-proof) -->
  <scm>
    <url>https://github.com/anujyog1/carrepo</url>
    <connection>scm:git:https://github.com/anujyog1/carrepo.git</connection>
    <developerConnection>scm:git:git@github.com:anujyog1/carrepo.git</developerConnection>
    <tag>HEAD</tag>
  </scm>

</project>
