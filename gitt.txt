git config --global user.name "Your Name"
git config --global user.email "you@example.com"
git config --global init.defaultBranch main

1
git restore <file>
# or (older)
git checkout -- <file>

2
git reset --soft HEAD~1

3
git log --oneline --graph --decorate --all

4
git checkout -b Feature/patientand

5
git push origin Feature/patientand
# First time set upstream:
git push -u origin Feature/patientand

6
mvn test

7
git branch -a

8
git checkout main
git pull origin main
git merge Feature/patientand
git push origin main


9
git push -u origin branchname

10
git remote -v
git remote set-url origin https://github.com/yourusername/carrepo.git
git remote -v

11
git fetch origin
git rebase origin/main
# or if on feature branch and you want to pull+rebase:
git pull --rebase origin main

12
git push origin --delete Feature/patientand

13
git apply file.patch
git add .
git commit -m "Apply patch: <brief description>"
git push origin branchname
